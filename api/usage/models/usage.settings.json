{
  "kind": "collectionType",
  "collectionName": "usage",
  "info": {
    "name": "Usage"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "name": {
      "type": "string",
      "required": true
    },
    "spaces_usages": {
      "via": "usage_id",
      "collection": "spaces-usages"
    }
  }
}
